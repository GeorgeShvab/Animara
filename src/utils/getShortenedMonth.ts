const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']

function getShortenedMonth(index: number) {
  return months[index]
}

export default getShortenedMonth
